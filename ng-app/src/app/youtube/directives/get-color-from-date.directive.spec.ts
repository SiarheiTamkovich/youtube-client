import { GetColourFromDateDirective } from './get-color-from-date.directive';

describe('GetColourFromDateDirective', () => {
  it('should create an instance', () => {
    const directive = new GetColourFromDateDirective();
    expect(directive).toBeTruthy();
  });
});
